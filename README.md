# 📚 Изучатор

> Образовательная платформа для учеников и преподавателей. Учи, Учись, Твори!

## 🚀 Стек технологий

- **Next.js 15.4** с App Router и Turbopack
- **React 19** с Server Components
- **TypeScript 5** в strict mode
- **Tailwind CSS 4** для стилей
- **Prisma** + PostgreSQL для данных
- **NextAuth v5** для аутентификации
- **TanStack Query 5** для кеширования
- **Framer Motion 12** для анимаций
- **shadcn/ui** + Radix UI для компонентов

## 📱 Mobile-First Design

Платформа разработана с приоритетом на мобильные устройства и планшеты:

- **Bottom Tab Bar** на мобильных устройствах (< 768px)
- **Drawer Sidebar** на планшетах (768px - 1024px)
- **Fixed Sidebar** на десктопе (> 1024px)
- **Touch-friendly** компоненты с минимальным размером 44px
- **Bottom Sheet** диалоги на мобильных

## 🔒 Соответствие законодательству РФ

- ✅ ФЗ-152 «О персональных данных»
- ✅ Политика конфиденциальности
- ✅ Пользовательское соглашение
- ✅ Cookie-уведомление
- ✅ Проверка возраста 14+
- ✅ Данные хранятся в РФ

## 🛠 Установка

### Требования

- Node.js 22+
- pnpm 9+
- PostgreSQL 16+
- Redis 7+ (опционально, для rate limiting в production)

### Локальная разработка

```bash
# Клонируем репозиторий
git clone https://github.com/your-org/izuchator.git
cd izuchator

# Устанавливаем зависимости
pnpm install

# Копируем переменные окружения
cp .env.example .env.local

# Генерируем Prisma клиент
pnpm prisma generate

# Запускаем миграции
pnpm prisma migrate dev

# Запускаем dev сервер
pnpm dev
```

### Docker

```bash
# Запуск с Docker Compose
docker compose up -d

# Только база данных
docker compose up postgres redis -d
```

## 📁 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API endpoints
│   ├── dashboard/         # Дашборд (защищённая зона)
│   ├── privacy/           # Политика конфиденциальности
│   └── terms/             # Пользовательское соглашение
├── components/            # React компоненты
│   ├── ui/               # UI-примитивы (shadcn/ui)
│   ├── auth/             # Компоненты авторизации
│   └── planner/          # Компоненты планера
├── hooks/                 # React хуки
├── lib/                   # Утилиты и конфигурации
│   ├── auth.ts           # NextAuth конфигурация
│   ├── database.ts       # Prisma клиент
│   └── validations.ts    # Zod схемы
└── middleware.ts          # Security middleware
```

## 🔐 Переменные окружения

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/izuchator"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"

# Email (для OTP)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user"
SMTP_PASS="password"
EMAIL_FROM="noreply@izuchator.ru"

# Redis (опционально)
REDIS_URL="redis://localhost:6379"
```

## 🧪 Тестирование

```bash
# E2E тесты (Playwright)
pnpm exec playwright test

# С UI
pnpm exec playwright test --ui

# Конкретный браузер
pnpm exec playwright test --project=chromium
```

## 📦 Сборка и деплой

```bash
# Сборка
pnpm build

# Запуск production
pnpm start

# Проверка типов
pnpm tsc --noEmit

# Линтинг
pnpm lint
```

## 🔄 CI/CD

GitHub Actions автоматически:

1. **Lint & Type Check** - проверка кода
2. **Build** - сборка приложения
3. **E2E Tests** - Playwright тесты
4. **Docker Build** - сборка Docker образа
5. **Deploy** - деплой на сервер

## 📐 Архитектурные решения

### Mobile-First

Все компоненты разрабатываются сначала для мобильных, затем адаптируются для десктопа:

```tsx
// Пример: размер кнопки
className="h-12 sm:h-10" // 48px на мобильных, 40px на десктопе
```

### Apple HIG Compliance

- Минимальный touch target: 44x44px
- Bottom navigation на мобильных
- Gesture-based interactions
- Safe area support

### Security

- Rate limiting на API
- CSP headers
- HTTPS-only в production
- Input validation с Zod

## 📄 Лицензия

Proprietary. All rights reserved.

## 👥 Команда

Разработано с ❤️ в России
